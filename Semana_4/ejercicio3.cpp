/*
    Ejecute el programa dandole dos palabras como argumento
    Ejemplo: ./prog "palabra 1" "palabra 2"

    Luego codifique la funcion bool is_bigger_than_word(char *palabra1, char* palabra2)
    que retorne si la palabra1 > palabra2 en orden lexicografico.
    Consideren que en caso de ser iguales si la palabra1 tiene mas caracteres que palabra2 entonces retorna true, false si no.
*/

bool is_bigger_than_word(char *palabra1, char* palabra2);

int main(int argc, char** argv) {
    
}